<view class="container">
  
  <view class="header-card">
    <view class="greeting">👋 早上好，园主</view>
    <view class="orchard-info">
      <view class="orchard-name">{{orchard.name}}</view>
      <view class="tags">
        <text class="tag">{{orchard.crop}}</text>
        <text class="tag">{{orchard.treeAge}}年树 · {{orchard.stage}}</text>
      </view>
    </view>
    <view class="weather-tip">🌤️ 今日天气适宜，建议巡园。</view>
  </view>

  <view class="action-area">
    <view class="big-btn" bindtap="startDiagnosis">
      <image src="/images/icons/search.png" class="btn-icon"></image>
      <view class="btn-text">
        <text class="main-text">智能诊断</text>
        <text class="sub-text">拍照/问诊 识别病害</text>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="section-header">
      <text class="title">最近记录</text>
      <text class="more">查看全部 ></text>
    </view>

    <view class="history-list">
      <block wx:for="{{historyList}}" wx:key="id">
        <view class="history-item" bindtap="goHistoryDetail" data-id="{{item.id}}">
          <view class="item-left">
            <view class="item-date">{{item.date}}</view>
          </view>
          <view class="item-right">
            <view class="item-result">{{item.result}}</view>
            <view class="item-desc">{{item.desc}}</view>
          </view>
          <view class="arrow">></view>
        </view>
      </block>
    </view>
  </view>

</view>